tags:
  managed_by: opentofu
  team: platform

sqs_queues:
  _templates:
    sqs_base:
      retention: 86400
      dlq: true
      visibility_timeout: 30

dynamodb_tables:
  _templates:
    dynamodb_base:
      billing_mode: PAY_PER_REQUEST

s3_buckets:
  _templates:
    s3_base:
      versioning: true
      encryption: AES256
